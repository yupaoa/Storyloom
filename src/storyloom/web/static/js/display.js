/* ═══════════════════════════════════════════════════════════════════
   display.js — DOM rendering functions

   Exports (Display namespace):
     appendSegment(text)           — add <p> to story area
     showChoices(choices) → Promise — render choice buttons, resolve on click
     clearChoices()                — remove choice panel + keyboard listeners
     showError(msg, onRetry, onQuit) — error modal with action buttons
     updateStatePanel(vars, changes) — sidebar state variables
     updateOutline(nodes)          — sidebar outline progress
     showAdventureLog(text)        — render adventure log
     _esc(s)                       — HTML entity escape
   ═══════════════════════════════════════════════════════════════════ */

const Display = {
    // appendSegment(text)
    // showChoices(choices) → Promise<key>
    // clearChoices()
    // showError(message, onRetry, onQuit)
    // updateStatePanel(vars, changes)
    // updateOutline(nodes)
    // showAdventureLog(text)
    // _esc(s) — HTML escape
};
